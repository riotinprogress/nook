# Dependencies
node_modules/

# Build caches / local tooling
.vite/
*.tsbuildinfo

# Local env
.env
.env.local
*.local

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Prior generated reports (not part of the app source)
NOOK_CODEBASE_REPORT.*
.abacus.donotdelete

# Note: dist/ is intentionally tracked — the single-file build in dist/index.html
# is a shipped deliverable for this project.
